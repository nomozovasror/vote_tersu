UNIVERSITY_NAME=Termiz davlat universiteti
UNIVERSITY_SHORT_NAME=TerDU
HEMIS_API_BASE_URL=https://student.tersu.uz
# Full endpoint for employee list
EXTERNAL_API_URL=https://student.tersu.uz/rest/v1/data/employee-list
# API token from HEMIS admin panel
EXTERNAL_API_TOKEN=

# Docker container prefix (unique per university to avoid conflicts)
# Examples: terdu, tuit, samdu, namdu
CONTAINER_PREFIX=terdu

# Public IP or domain of your server
SERVER_HOST=213.230.97.43
# API Backend port (default: 2014)
API_PORT=2014
# Web Frontend port (default: 2013)
WEB_PORT=2013

SECRET_KEY=uE1tujuAokux6GcmYx3VBumn86JqWG73

# JWT Algorithm (recommended: HS256)
ALGORITHM=HS256

# Token expiration in minutes (1440 = 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-strong-admin-password-here

DATABASE_URL=sqlite:///./data/voting.db

# IMPORTANT: WebSocket requires single worker (WEB_CONCURRENCY=1)
# Multi-worker breaks WebSocket broadcast (each worker has own ConnectionManager)
# For multi-worker, implement Redis pub/sub
WEB_CONCURRENCY=1

# Performance tuning (optimized for 200+ concurrent users)
# Maximum WebSocket connections per event (recommended: 1000 for 200 users)
MAX_CONNECTIONS_PER_EVENT=1000
# Maximum total WebSocket connections (recommended: 5000)
MAX_TOTAL_CONNECTIONS=5000

# NOTE: With current optimizations (uvloop, ulimits, session fixes):
# - 1 worker can handle 200-250 concurrent users
# - For 500+ users, use Redis pub/sub + multiple workers





